# Admiral runtime state (ephemeral, session-scoped)
.admiral/session_state.json
.admiral/session_state.json.lock
.admiral/event_log.jsonl

# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Adoption simulation — track current results and reports (the deliverables),
# but not history snapshots (temporal data for local trend analysis)
research/adoption-sim/results/history/
research/adoption-sim/data/fortune500-scored.json

# Monitor: state and digests are committed deliberately via CI,
# but seed candidates should be reviewed before committing
# aiStrat/monitor/digests/seed-candidates-*.py
